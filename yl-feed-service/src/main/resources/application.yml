spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:feed_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
    username: admin
    password: admin
